<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style/style.css" />

    <title>My Ingredients</title>
  </head>
  <body>


  <%- include('../partials/_1navbar.ejs') %>
    <h1>Check Out the Food Here</h1>
    <ul>
    <% pantry.forEach((food)=>{ %>
    <a href="/users/<%= user._id %>/foods/<%= food._id %>">
      <li><%= food.name %> </li>
    </a>
    <% }) %>
  </ul>
  </body>
</html>
